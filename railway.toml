# Deploy config for the protein-tracking Telegram bot.
# Railway runs startCommand WITHOUT a shell — no $PORT expansion, but we don't need it
# (polling bots have no inbound HTTP).
[deploy]
startCommand = "proteinbot"
